# Copy to frontend/.env.local. Restart Vite after changing these values.
# A page URL with ?mode=demo or ?mode=api overrides the default below.
VITE_DATA_MODE=demo
VITE_API_BASE_URL=http://localhost:8000/api
